import React from 'react';
import ReactDOM from 'react-dom';
import './index.css'; // Opcional: Estilo global para tu aplicación
import App from './App';

ReactDOM.render(
    <React.StrictMode>
        <App />
    </React.StrictMode>,
    document.getElementById('root')
);
